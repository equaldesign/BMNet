$(document).ready(function(){
  var options = {
      zoomWidth: 350,
      zoomHeight: 350,
      zoomType:'reverse',
      xOffset: 10,
      yOffset: 0,
      position: "right" //and MORE OPTIONS
};
  $('.zoom').jqzoom(options);
});
